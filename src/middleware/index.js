import {Router} from 'express';

/*
Router middleware which returns an router
object as an api object
*/

export default ({config,db})=>{

    let api = Router();

    //add middleware

    return api;
};